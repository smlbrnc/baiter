/** Shared dashboard / bot card header vignette (matches across app). */
export const HEADER_RADIAL_GRADIENT =
  "radial-gradient(ellipse 70% 55% at 70% -30%, color-mix(in oklch, var(--muted-foreground) 14%, transparent), transparent)"

/** Outer shell for card surfaces (dashboard header, bot rows, form). */
export const CARD_SHELL_CLASS =
  "ring-border/35 bg-card text-card-foreground overflow-hidden rounded-md shadow-xs ring-1"
